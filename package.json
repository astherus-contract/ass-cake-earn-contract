{
  "name": "ass-cake-earn-contract",
  "version": "1.0.0",
  "description": "**Foundry is a blazing fast, portable and modular toolkit for Ethereum application development written in Rust.**",
  "scripts": {
    "prepare": "husky",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:check:format": "prettier --check **/*.sol",
    "lint:check:solhint": "solhint **/*.sol",
    "check": "yarn lint:check:format && yarn lint:check:solhint",
    "lint:fix:format": "prettier --write **/*.sol",
    "fix": "yarn lint:fix:format"
  },
  "author": "ass",
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.4",
    "prettier": "^3.2.5",
    "prettier-plugin-solidity": "^1.3.1",
    "solhint": "^5.0.1",
    "solhint-plugin-prettier": "^0.1.0"
  }
}
